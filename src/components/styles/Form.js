import styled from 'styled-components';

const Form = styled.form`
  div {
    display: block;
    color: green;

    label,
    input {
      display: inline-block;
    }
  }
`;

export default Form;
